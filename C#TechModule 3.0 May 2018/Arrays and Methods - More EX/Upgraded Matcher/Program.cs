using System;
using System.Linq;

namespace Upgraded_Matcher
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] n = Console.ReadLine().Split().Select(int.Parse).ToArray();

            int ind = 0;
            int sum = 0;

            while (ind > 0 && ind < numbers.Length)
            {

            }
        }
    }
}
